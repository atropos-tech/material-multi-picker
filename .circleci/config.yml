version: 2
jobs:
  build:
    docker:
      - image: circleci/node:11.9.0
    steps:
      - checkout
      - run: npm install
      - run: npm run lint
      - run: npm test
      - persist_to_workspace:
        root: .
        paths: coverage
